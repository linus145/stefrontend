'use client';

import React from 'react';
import { SettingsView } from '@/components/dashboard/settings/settings-view';

export default function SettingsPage() {
  return (
    <SettingsView isCollapsed={false} />
  );
}
